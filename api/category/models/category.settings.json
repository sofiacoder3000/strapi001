{
  "kind": "collectionType",
  "collectionName": "categories",
  "info": {
    "name": "category",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "name": {
      "type": "string",
      "maxLength": 20,
      "minLength": 1,
      "unique": true,
      "required": true
    },
    "quizzes": {
      "via": "categories",
      "collection": "quiz",
      "dominant": true
    },
    "allowed": {
      "type": "enumeration",
      "enum": [
        "over13",
        "over18",
        "all"
      ]
    }
  }
}
